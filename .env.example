# Flight Tracker Environment Variables

# Resend API Key (get one free at https://resend.com)
# Free tier: 100 emails/day, 3000 emails/month
RESEND_API_KEY=re_xxxxxxxxx

# Optional: override where the SQLite DB file lives.
# Useful in Railway (mount a volume and point DB_PATH at it for persistence).
DB_PATH=./data/flights.db

# Puppeteer: on Railway with nixPkgs chromium, this is available on PATH as `chromium`
PUPPETEER_EXECUTABLE_PATH=chromium
